AUTH_SECRET=""                          # run: openssl rand -base64 32
HMAC_SECRET_KEY=""                      # run: openssl rand -base64 32
AUTH_URL=""                             # ngrok URL in dev, prod URL in production

# World ID / Developer Portal
NEXT_PUBLIC_APP_ID="app_xxxxxxxxxxxx"   # app_id from developer.world.org
NEXT_PUBLIC_WLD_ACTION_NAME="payment-verification"  # incognito action you created
NEXT_PUBLIC_WLD_RP_ID="rp_xxxxxxxxxxxx"             # rp_id from Developer Portal
RP_SIGNING_KEY="xxxxxxxxxxxxxxxxxxxx"               # signing key from Developer Portal (keep secret — backend only)